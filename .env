# Base URL for UI tests
BASE_URL=base_test_url

# Base URL for API tests (if different from UI)
API_BASE_URL=api_base_test_url

# Test environment (dev, staging, prod)
#TEST_ENV=staging

# Test execution settings
TIMEOUT=30000
WORKERS=4
RETRIES=0

# Performance test thresholds
#PERF_LOAD_TIME_THRESHOLD=3000
#PERF_FIRST_PAINT_THRESHOLD=1000